(function () {
  const protocol = "https://";
  const url = "api.weatherapi.com/v1/current.json?";
  const apiStart = "key=";

  const apiKey = "";

  const itemStart = "&q=";
  const defaultLocation = "garland";

  function getWeather(location) {}
})();
